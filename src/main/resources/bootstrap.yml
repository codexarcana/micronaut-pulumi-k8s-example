micronaut:
  application:
    name: pulumiK8sApp
  config-client:
    enabled: true
kubernetes:
  client:
    secrets:
      enabled: true
      labels:
        - app: mnpluspulumi
    config-maps:
      labels:
        - app: mnpluspulumi
